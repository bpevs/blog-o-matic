import { expect } from "chai";
import { spy } from "sinon";
import promisify from "app/utilities/promisify";
import gitConnect from "app/utilities/gitConnect";

describe("GitConnect", function() {
  beforeEach(function() {});
  it("Should be able to clone a repository");
  it("Should be able to send pull requests");
  it("Should be able to checkout from a branch");
  it("Should be able to reset a repository to a branch from the origin");
});
